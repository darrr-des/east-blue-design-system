# Component assessment gap report

Total: **79** components · **79** done · **0** partial · **0** untouched

Sorted by **fewest gaps first** — easier wins at the top.

---

## ✅ Complete (79)

- **accordion** — 2 cards, every section + native code present
- **action-list-counter** — 6 cards, every section + native code present
- **action-list-description** — 3 cards, every section + native code present
- **action-list** — 3 cards, every section + native code present
- **ad-space** — 3 cards, every section + native code present
- **alert** — 2 cards, every section + native code present
- **amount-text-field** — 6 cards, every section + native code present
- **avatar-group** — 4 cards, every section + native code present
- **avatar** — 3 cards, every section + native code present
- **badge** — 4 cards, every section + native code present
- **banner** — 3 cards, every section + native code present
- **bottom-sheet** — 2 cards, every section + native code present
- **button** — 3 cards, every section + native code present
- **callout** — 5 cards, every section + native code present
- **carousel-card** — 3 cards, every section + native code present
- **carousel-discount-card** — 3 cards, every section + native code present
- **carousel-item** — 2 cards, every section + native code present
- **chat-field** — 2 cards, every section + native code present
- **checkbox** — 2 cards, every section + native code present
- **chip** — 4 cards, every section + native code present
- **counter** — 3 cards, every section + native code present
- **date-picker-group** — 3 cards, every section + native code present
- **date-picker-item** — 7 cards, every section + native code present
- **date-picker** — 5 cards, every section + native code present
- **dropdown-item-group** — 1 card, every section + native code present
- **dropdown-item** — 5 cards, every section + native code present
- **dropdown** — 4 cards, every section + native code present
- **empty-state** — 2 cards, every section + native code present
- **footer** — 7 cards, every section + native code present
- **generic-card** — 2 cards, every section + native code present
- **generic-transaction-card** — 3 cards, every section + native code present
- **header-centered** — 2 cards, every section + native code present
- **header-transaction** — 2 cards, every section + native code present
- **header-with-logo** — 2 cards, every section + native code present
- **header** — 5 cards, every section + native code present
- **horizontal-voucher** — _intentionally cardless (verdict: `consolidate`)_
- **inline-message** — 3 cards, every section + native code present
- **inline-text** — 5 cards, every section + native code present
- **input-field** — 4 cards, every section + native code present
- **labeled-field** — 4 cards, every section + native code present
- **list-item-asset** — 1 card, every section + native code present
- **list-item** — 3 cards, every section + native code present
- **list** — _intentionally cardless (verdict: `remove`)_
- **menu-grid** — 3 cards, every section + native code present
- **modal** — 3 cards, every section + native code present
- **month-year-picker-item** — 3 cards, every section + native code present
- **onboarding-tooltip** — 4 cards, every section + native code present
- **overlay** — 1 card, every section + native code present
- **progress-bar** — 1 card, every section + native code present
- **radio-button-with-label** — 4 cards, every section + native code present
- **radio-button** — 1 card, every section + native code present
- **recipient-field** — 4 cards, every section + native code present
- **search-field** — 2 cards, every section + native code present
- **select-field** — 4 cards, every section + native code present
- **stepper-bullet** — 1 card, every section + native code present
- **stepper-circular** — 1 card, every section + native code present
- **stepper-dash** — 1 card, every section + native code present
- **subtext-message** — 3 cards, every section + native code present
- **tab-item** — 4 cards, every section + native code present
- **table-scheduling** — 3 cards, every section + native code present
- **table-transaction** — 2 cards, every section + native code present
- **table** — 2 cards, every section + native code present
- **tabs** — 3 cards, every section + native code present
- **terms-conditions-accordion** — _intentionally cardless (verdict: `remove`)_
- **text-area** — 4 cards, every section + native code present
- **title-bar** — 2 cards, every section + native code present
- **toast-with-button** — 4 cards, every section + native code present
- **toast** — 4 cards, every section + native code present
- **toggle-with-label** — 3 cards, every section + native code present
- **toggle** — 4 cards, every section + native code present
- **tooltip-blurred** — 4 cards, every section + native code present
- **tooltip-v2** — 8 cards, every section + native code present
- **upload-file** — 5 cards, every section + native code present
- **vertical-voucher** — _intentionally cardless (verdict: `consolidate`)_
- **view-only-field** — 4 cards, every section + native code present
- **visual-popup** — 3 cards, every section + native code present
- **voucher-asset** — 3 cards, every section + native code present
- **voucher-card-horizontal** — 3 cards, every section + native code present
- **voucher-details** — _intentionally cardless (verdict: `product-layer`)_

---

## 🟡 Partial (0) — sorted easiest first

